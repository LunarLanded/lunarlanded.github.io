# MkDocs build output
site/
.cache/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# macOS
.DS_Store
._*

# Editors
.vscode/
.idea/

# Obsidian — vault config is machine-local (workspace layout, open panes).
# Remove this line if you'd rather sync settings across machines.
.obsidian/

# Lightroom / photo working files — keep the repo light.
# Only web-sized exports belong in docs/assets/images/
*.lrcat
*.lrcat-*
*.xmp
*.dng
*.CR2
*.CR3
*.NEF
*.ARW
*.psd
*.tif
*.tiff

# Transfer artifacts
_to_delete/
