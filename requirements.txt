# Pinned so CI builds stay reproducible.
#
# The `mkdocs<2` pin is deliberate: MkDocs 2.0 is a backward-incompatible
# rewrite that drops the plugin system and the theming system Material depends
# on. Material for MkDocs 9.x targets MkDocs 1.x. Don't loosen this without
# reading https://squidfunk.github.io/mkdocs-material/blog/2026/02/18/mkdocs-2.0/
mkdocs>=1.6,<2
mkdocs-material==9.*
mkdocs-glightbox==0.4.*
